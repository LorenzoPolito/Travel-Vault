{
  "name": "travel-vault-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "node scripts/sync-content.mjs && astro dev",
    "build": "node scripts/sync-content.mjs && astro build && npx pagefind --site dist --output-subdir _pagefind",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "leaflet": "^1.9.4",
    "pagefind": "^1.4.0"
  }
}
